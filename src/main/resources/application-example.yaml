spring:
  application:
    name: hybridAuth
  datasource:
    url: jdbc:mysql://localhost:3306/your_database
    username: YOUR_USERNAME
    password: YOUR_PASSWORD
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
